Apex code TO fetch data Using SOQL
----------------------------------------------------------------------------------------------------------------------------------------------
public class SOQL {
    public static void main()
    {
    List<apex_Employes__c> EmployesList = new List<apex_Employes__c>();
    EmployesList= [SELECT Id, Name, FROM
                   Department WHERE createdDate = today;
    System.debug('We have total '+InvoiceList.size()+' Records in List');
    
    for (objEmployes: EmployesList) {
        System.debug('Employes'); 
        
    }
}
}
------------------------------------------------------------------------------------------------------------------------------------------------
# Code in Development Mode #

